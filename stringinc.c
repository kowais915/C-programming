#include <stdio.h>

int main(){
    // int x = 1;
    //printing the address of the stored data

    // printf("The address of the variable is %p: ", &x);

char codes[2];
scanf("%d", codes);
printf("%d", codes[0]);


}
